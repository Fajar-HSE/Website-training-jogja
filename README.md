# Training Jogja — Frontend (scaffold)

Minimal Next.js + TypeScript scaffolding created in workspace. To run locally:

```bash
npm install
npm run dev
```

Next steps:
- Install UI libs: `npm i @shadcn/ui framer-motion`
- Configure WPGraphQL adapter
- Implement pages and components per System Design
